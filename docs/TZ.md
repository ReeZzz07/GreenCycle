Ниже — **полное, структурированное и готовое к разработке техническое задание (ТЗ)** для проетирования системы учёта, адаптированное под бизнес-процесс (закупка → продажа → выкуп туи), требования к прозрачности между партнёрами, российскую юрисдикцию и полный цикл учёта.

---

# 📑 **ТЕХНИЧЕСКОЕ ЗАДАНИЕ**  
**Проект**: Система полного учёта закупок, продаж, выкупа и финансов для бизнеса по продаже декоративных растений  
**Кодовое имя**: **GreenCycle**  
**Версия**: 1.0  
**Дата**: 10 ноября 2025 г.

---

## 1. **Цель проекта**

Разработать веб-приложение (PWA) для ведения **полного управленческого и бухгалтерского учёта** в бизнесе по оптовой продаже декоративных растений с последующим выкупом через 3–4 года.  
Система должна обеспечивать **прозрачность между партнёрами**, **точный учёт активов**, **автоматизацию цикла выкупа** и **формирование финансовых отчётов в реальном времени**.

---

## 2. **Бизнес-требования**

### 2.1. Основные процессы
- Закупка партий саженцев у поставщиков.
- Хранение на физическом складе с учётом остатков в реальном времени.
- Продажа мелким и крупным оптом с фиксацией данных клиента и привязкой к партии.
- Отслеживание клиентов, у которых возможен выкуп через 3–4 года.
- Выкуп саженцев у клиентов с регистрацией обратной операции.
- Учёт всех финансовых операций, включая личные изъятия партнёров.
- Формирование бухгалтерских документов для ИП (УСН 6%).

### 2.2. Участники
- **2 партнёра** — равные права администратора.
- **Сотрудники** — менеджеры, бухгалтер, логист.
- **Разработчик** — супер-админ (технический доступ).

---

## 3. **Функциональные требования**

### 3.1. **Модуль «Закупки»**
- Создание карточки поставки: поставщик, дата, сумма, документы.
- Регистрация партии: вид, сорт, размер (в см.), горшок (P9), кол-во, закупочная цена за ед.
- Автоматический расчёт себестоимости и общей суммы.
- Привязка партии к поставке.

### 3.2. **Модуль «Склад»**
- Учёт остатков по каждой партии в реальном времени.
- Списание: дата, причина (брак, повреждение), количество, стоимость.
- Отображение общего остатка по ассортименту.

### 3.3. **Модуль «Продажи»**
- Карточка клиента:
  - ФИО, телефон, email (опционально)
  - Полный адрес
  - Тип: частное лицо / организация
  - Дата первой покупки
- Создание заказа: выбор партий, цена продажи, итоговая сумма.
- Генерация документов: счёт, акт приёма-передачи, товарная накладная.
- Привязка продажи к клиенту и к конкретной партии.

### 3.4. **Модуль «Выкуп» (ключевой)**
- Автоматическая постановка задачи на выкуп через **3–4 года** после продажи.
- Возможность корректировки даты выкупа вручную.
- CRM-статусы клиента:
  - «Ожидает выкуп»
  - «Связан»
  - «Отказался»
  - «Выкуп завершён»
- Регистрация выкупа:
  - Дата
  - Клиент
  - Кол-во выкупленных саженцев
  - Цена выкупа за ед.
  - Состояние (качество)
  - Привязка к исходной продаже
- Уведомления:
  - Внутрисистемные (в интерфейсе)
  - Email-уведомления за **60, 30 и 7 дней** до даты выкупа

### 3.5. **Модуль «Финансы и бухгалтерия»**
- Учёт по типам счётов:
  - Наличная касса
  - Расчётный счёт
  - Прочие (СБП и др.)
- Операции:
  - Приход (от клиентов)
  - Расход (закупки, логистика)
  - Личное изъятие партнёров:
    - Кто из партнёров
    - Сумма или кол-во товара
    - Дата
    - Тип (деньги / товар по себестоимости)
- Автоматический расчёт чистой прибыли с учётом всех операций.
- Генерация бухгалтерских проводок для УСН 6%.
- Ежедневный финансовый свод: остаток по счетам, движение за день/неделю/месяц.

### 3.6. **Модуль «Отчёты и аналитика»**
- Готовые отчёты:
  - Прибыль по поставке / клиенту / периоду
  - Остатки на складе
  - Прогноз выкупа (объём, потенциальная маржа)
  - Движение денежных средств
  - Активность клиентов
- Конструктор отчётов:
  - Выбор полей
  - Фильтрация (по дате, клиенту, типу операции и др.)
  - Группировка и агрегация (сумма, среднее, кол-во)
- Экспорт в **PDF и Excel**.
- Все отчёты — **в реальном времени**.
- Печать документов и отчётов из интерфейса.

### 3.7. **Модуль «Пользователи и права»**
- Роли и права:
  | Роль | Доступ |
  |------|--------|
  | **Супер-админ** | Полный доступ + технические настройки (бэкенд, логи) |
  | **Админ (2 партнёра)** | Все бизнес-модули, кроме технических |
  | **Менеджер** | Клиенты, продажи, CRM |
  | **Бухгалтер** | Финансы, отчёты, документы |
  | **Логист** | Склад, списания, остатки |
- Разделение прав через CRUD-матрицу (Create, Read, Update, Delete).
- Обязательная авторизация. Поддержка 2FA (опционально).

### 3.8. **Технические требования к интерфейсу**
- **Веб-приложение** с поддержкой **PWA**:
  - Работает на ПК, ноутбуках, планшетах, смартфонах.
  - Возможность добавить на главный экран (iOS/Android).
  - Офлайн-режим: кэширование последних отчётов и форм.
- Поддержка всех современных браузеров.
- Адаптивный дизайн.
- SSL-шифрование (HTTPS).

---

## 4. **Нефункциональные требования**

- **Производительность**: загрузка страниц < 1.5 с при 100 одновременных пользователях.
- **Надёжность**: uptime ≥ 99.5%.
- **Безопасность**: хранение паролей (bcrypt), защита от XSS/CSRF, регулярные бэкапы.
- **Резервное копирование**: ежедневный экспорт БД + файлов (сохраняется 7 копий).
- **Локализация**: интерфейс на русском языке.

---

## 5. **Технологический стек**

| Компонент | Технология |
|----------|-----------|
| **Фронтенд** | React 18 + TypeScript + Vite + TanStack Query |
| **UI-библиотека** | Mantine (адаптив, темизация, компоненты для таблиц/форм) |
| **PWA** | Workbox + manifest.json |
| **Бэкенд** | NestJS (Node.js) — модульная архитектура, DI, валидация |
| **База данных** | PostgreSQL 15+ (поддержка JSONB для гибких полей) |
| **Аутентификация** | JWT + refresh tokens (с опцией подключения Auth0 позже) |
| **Хостинг (рекомендуемый)** | **Timeweb Cloud (VDS)** — от 190 ₽/мес, Москва |
| **Дополнительно** | Nginx (реверс-прокси), PM2 (менеджер процессов), Let’s Encrypt (SSL) |

> 💡 Размещение на shared-хостинге **недопустимо** — требуется VDS/VPS с root-доступом.

---

## 6. **Этапы реализации**

| Этап | Срок | Результат |
|------|------|----------|
| **Проектирование** | 1–2 недели | ER-диаграмма БД, API-спецификация, UI-макеты (Figma) |
| **Разработка** | 10–12 недель | Полнофункциональное приложение по всем модулям |
| **Тестирование** | 1 неделя | QA, UAT с партнёрами |
| **Деплой и настройка** | 3–5 дней | Развёртывание на VDS, настройка бэкапов, SSL, домена |
| **Обучение и сдача** | 1 день | Инструкция, демо-сессия |

> 💡 Деплой — **только после полной готовности** (без MVP).

---

## 7. **Интеграции и документация**

- Генерация PDF через **pdfmake** или **Puppeteer**.
- Экспорт Excel — **SheetJS (xlsx)**.
- Email-уведомления — **Nodemailer** + SMTP (можно через Яндекс 360 или Mail.ru для бизнеса).
- Документация:
  - Техническая (для разработчика)
  - Пользовательская (для партнёров и сотрудников)

---

## 8. **Ограничения и допущения**

- Поставщики не регистрируются в системе — только как справочник.
- Клиенты не имеют личного кабинета (работа только через админов/менеджеров).
- Все финансовые операции подтверждаются вручную (без автоматических платёжных шлюзов на старте).

---

## 9. **Критерии приёмки**

Система считается готовой, если:
- Все модули реализованы согласно разделу 3.
- Отчёты отображают актуальные данные в реальном времени.
- Два партнёра могут вести раздельный учёт изъятий без конфликтов.
- Возможен экспорт всех данных в Excel/PDF.
- Приложение работает на всех устройствах через браузер.
- Развёрнуто на выбранном хостинге с HTTPS и резервным копированием.


## **10. Архитектура базы данных (ER-диаграмма)**

Все таблицы используют **PostgreSQL**. Первичные ключи — `id SERIAL PRIMARY KEY`. Связи — через внешние ключи. Даты — в формате `TIMESTAMP WITH TIME ZONE`.

### **10.1. Пользователи и роли**
```sql
users
- id
- email (уникальный)
- password_hash
- full_name
- role_id → roles.id
- created_at

roles
- id
- name (‘super_admin’, ‘admin’, ‘manager’, ‘accountant’, ‘logistic’)
```

### **10.2. Поставки и партии**
```sql
suppliers
- id
- name
- contact_info

shipments (поставки)
- id
- supplier_id → suppliers.id
- arrival_date
- total_cost
- document_url
- created_by → users.id

batches (партии)
- id
- shipment_id → shipments.id
- plant_type (‘Thuja Smaragd’ и др.)
- size_cm_min, size_cm_max
- pot_type (‘P9’)
- quantity_initial
- quantity_current (обновляется при продаже/списании)
- purchase_price_per_unit
- created_at
```

### **10.3. Клиенты**
```sql
clients
- id
- full_name
- phone
- email
- address_full
- client_type (‘individual’, ‘wholesaler’)
- first_purchase_date
- created_at
```

### **10.4. Продажи**
```sql
sales
- id
- client_id → clients.id
- sale_date
- total_amount
- status (‘completed’, ‘cancelled’)
- created_by → users.id

sale_items
- id
- sale_id → sales.id
- batch_id → batches.id
- quantity
- sale_price_per_unit
```

### **10.5. Выкуп**
```sql
buybacks (выкупы)
- id
- original_sale_id → sales.id
- client_id → clients.id
- planned_date
- actual_date (NULL если не выкуплен)
- status (‘planned’, ‘contacted’, ‘declined’, ‘completed’)
- notes
- created_by → users.id

buyback_items
- id
- buyback_id → buybacks.id
- original_sale_item_id → sale_items.id
- quantity
- buyback_price_per_unit
- condition_notes
```

> 💡 Связь `buyback → sale` позволяет отслеживать: *«у этого клиента мы купили в 2025 → выкупаем в 2028»*.

### **10.6. Финансы**
```sql
accounts
- id
- name (‘Касса’, ‘Р/счёт’, ‘СБП’)
- type (‘cash’, ‘bank’, ‘other’)
- balance (рассчитывается триггерами или агрегацией)

transactions
- id
- account_id → accounts.id
- amount (положительное — приход, отрицательное — расход)
- type (
    ‘purchase’,         -- закупка
    ‘sale’,             -- продажа
    ‘buyback’,          -- выкуп
    ‘write_off’,        -- списание
    ‘partner_withdrawal’-- изъятие партнёра
  )
- description
- linked_entity_id   -- ссылка на shipment/sale/buyback/write_off/partner_withdrawal
- linked_entity_type
- created_by → users.id
- created_at

partner_withdrawals
- id
- user_id → users.id (только админы)
- type (‘cash’, ‘goods’)
- amount_or_quantity
- cost_value         -- если товар: расчёт по себестоимости на момент изъятия
- reason
- created_at
```

> 💡 Все движения денег/товаров проходят через `transactions` → обеспечивается **единая финансовая история**.

### **10.7. Уведомления**
```sql
notifications
- id
- user_id → users.id
- client_id → clients.id (опционально)
- buyback_id → buybacks.id
- message
- is_read
- due_date
- created_at
```

### **10.8. Отчёты (логика в приложении, не в БД)**
- Все отчёты строятся **динамически** через агрегацию данных из указанных таблиц.
- Нет отдельных таблиц для отчётов — только **представления (views)** при необходимости.

---

## **11. Описание API-эндпоинтов (REST, JSON)**

Все эндпоинты требуют авторизации (`Bearer <JWT>`). Ответы в формате:
```json
{ "data": ..., "error": null }
```

### **11.1. Аутентификация**
- `POST /auth/login` — вход (email + пароль)
- `POST /auth/refresh` — обновление токена

### **11.2. Поставки и склад**
- `GET /shipments` — список поставок
- `POST /shipments` — создать поставку
- `POST /batches` — добавить партию к поставке
- `POST /write-offs` — списать брак

### **11.3. Клиенты**
- `GET /clients` — список клиентов (+ фильтры)
- `POST /clients` — создать клиента
- `PATCH /clients/:id` — обновить

### **11.4. Продажи**
- `POST /sales` — создать продажу (с позициями)
- `GET /sales?client_id=...` — история клиента
- `GET /sales/:id/documents` — PDF-документы

### **11.5. Выкуп**
- `GET /buybacks/upcoming` — выкупы в ближайшие 90 дней
- `POST /buybacks` — запланировать выкуп (на основе sale_id)
- `PATCH /buybacks/:id/status` — обновить статус
- `POST /buybacks/:id/complete` — завершить выкуп

### **11.6. Финансы**
- `GET /accounts` — список счетов с балансами
- `GET /transactions?date_from=...&type=...` — движение средств
- `POST /partner-withdrawals` — зафиксировать изъятие партнёра

### **11.7. Отчёты**
- `POST /reports/custom` — тело запроса: `{ fields: [...], filters: {...}, groupBy: [...] }`
- `GET /reports/profit-by-client`
- `GET /reports/inventory`
- `GET /reports/buyback-forecast`

> Все отчёты возвращают данные **в реальном времени**.

### **11.8. Уведомления**
- `GET /notifications?unread=true`
- `PATCH /notifications/:id/read`

### **11.9. Пользователи (только super_admin / admin)**
- `GET /users`
- `POST /users`
- `PATCH /users/:id/role`

---

## ✅ Заключение

Данное ТЗ включает:
- Полное описание бизнес-логики
- Функциональные и нефункциональные требования
- Технологический стек
- ER-диаграмму базы данных
- Спецификацию API

